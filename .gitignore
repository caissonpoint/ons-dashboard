# Downloaded ONS source files and the derived store - both rebuildable,
# both far too large for git. CI keeps them in the Actions cache instead.
raw/
data/
site/
ons_dashboard.html

.venv/
__pycache__/
*.pyc
node_modules/
*.png

# Personal-machine trigger scripts -- these embed a GitHub PAT, never commit them.
run_refresh.local.bat
*.local.bat
